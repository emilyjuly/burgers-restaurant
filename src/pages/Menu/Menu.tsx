// src/pages/Menu.jsx
import React, { useEffect } from 'react';
import { useSelector, useDispatch } from 'react-redux';
import { fetchStyles } from '../../store/slices/menuSlice';

const Menu = () => {
  return (
    <div>
      <h1>Menu</h1>
    </div>
  );
};

export default Menu;
